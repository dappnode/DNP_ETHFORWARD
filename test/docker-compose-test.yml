version: "3.4"

services:
  test:
    build: ./build-test
    image: test
    container_name: Test
    dns: 172.33.1.2
    networks:
      - test_build_network

networks:
  test_build_network:
    external: true
